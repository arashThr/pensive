{{template "header" .}}

<div class="px-6 py-16 max-w-2xl mx-auto">
  <!-- Search Box -->
  <div class="relative">
    <input 
      type="text" 
      placeholder="Search your bookmarks..." 
      class="w-full p-4 border border-gray-300 focus:border-red-600 focus:ring-1 focus:ring-red-600/20 outline-none bg-white text-gray-900 rounded-md shadow-sm transition-all duration-200 placeholder:text-gray-500" 
      id="search-input"
      hx-get="/bookmarks/search" 
      hx-target="#results" 
      hx-trigger="input delay:300ms" 
      hx-indicator=".htmx-indicator"
      name="query" 
    />
    <span class="htmx-indicator absolute right-3 top-1/2 -translate-y-1/2 text-gray-500 hidden">⌛</span>
  </div>
  <div class="mt-4 flex justify-between items-center">
    <p class="text-sm text-gray-500">Start typing to search your saved bookmarks.</p>
    <a href="/bookmarks/new" class="text-sm text-red-600 hover:text-red-700 font-medium transition-colors">+ Add Bookmark</a>
  </div>

  <!-- Results Area -->
  <div id="results" class="mt-6 bg-white rounded-lg shadow-sm p-6">
    <p class="text-gray-500 text-sm">No results yet. Type above to search.</p>
  </div>

  <!-- Subscription Link -->
  <div class="mt-6 text-right">
    <a href="/users/subscribe" class="text-sm text-red-600 hover:text-red-700 font-medium transition-colors">Subscribe for more features</button>
  </div>
</div>

{{template "footer" .}}