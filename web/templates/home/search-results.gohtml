{{if .HasResults}}
  <div class="bg-white border border-gray-200 rounded-xl shadow-sm">
    <div class="p-6 border-b border-gray-200">
      <div class="flex items-center justify-between">
        <h2 class="text-xl font-bold text-gray-900">Search Results</h2>
        <span class="text-sm font-semibold text-gray-600">
          {{len .Bookmarks}} result{{if ne (len .Bookmarks) 1}}s{{end}} for "{{.Query}}"
        </span>
      </div>
    </div>
    
    <div class="divide-y divide-gray-100">
      {{range .Bookmarks}}
        <a href="/bookmarks/{{.Id}}/edit" class="block hover:bg-gray-50 transition-colors">
          <div class="flex items-start gap-4 p-6">
            <!-- Favicon -->
            <div class="w-5 h-5 flex-shrink-0 mt-0.5">
              <img 
                src="https://www.google.com/s2/favicons?domain={{.Link}}&sz=64" 
                alt="{{.Title}}" 
                class="w-full h-full" 
                onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';" 
              />
              <div class="w-full h-full bg-gray-100 flex items-center justify-center hidden">
                <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
              </div>
            </div>
            
            <!-- Content -->
            <div class="flex-1 min-w-0">
              <h3 class="font-semibold mb-1 text-gray-900 hover:text-violet-600 transition-colors line-clamp-2 break-words">
                {{.Title}}
              </h3>
              <p class="text-xs mb-2 text-gray-500">{{.Hostname}}</p>
              {{if .Headline}}
                <p class="text-sm leading-relaxed mb-2 text-gray-600 line-clamp-2">{{.Headline | safe}}</p>
              {{end}}
              <span class="text-xs text-gray-500">{{.CreatedAt.Format "Jan 02, 2006"}}</span>
            </div>
          </div>
        </a>
      {{end}}
    </div>
  </div>
{{else}}
  <!-- Empty Search Results -->
  <div class="bg-gradient-to-br from-gray-50 to-gray-100 border border-gray-200 rounded-xl p-12 text-center">
    <div class="w-16 h-16 bg-gray-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
      <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
      </svg>
    </div>
    <h3 class="text-xl font-bold mb-3 text-gray-900">No results for "{{.Query}}"</h3>
    <p class="mb-8 max-w-sm mx-auto text-gray-600 leading-relaxed">
      Try different keywords or check your spelling. Your search will improve as you add more bookmarks.
    </p>
    <div class="space-y-3">
      <div class="flex flex-col sm:flex-row gap-3 justify-center items-center">
        <a href="/bookmarks/new" class="bg-violet-600 text-white font-semibold py-3 px-6 rounded-lg hover:bg-violet-700 transition-colors">
          Add new bookmark
        </a>
        <a href="/bookmarks" class="border border-gray-300 text-gray-700 font-semibold py-3 px-6 rounded-lg hover:bg-gray-50 transition-colors">
          Browse all bookmarks
        </a>
      </div>
    </div>
  </div>
{{end}} 